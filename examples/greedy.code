change = 0.41
change = int(change * 100)
coin_count = 0
coins = [25, 10, 5, 1]

for (i = 0; i < length(coins); i++)
  coin = coins[i]
  while change >= coin
    change -= coin
    coin_count++

print(coin_count)
